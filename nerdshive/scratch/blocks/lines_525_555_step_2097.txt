                        <div className="flex justify-between text-sm border-t pt-2 mt-2">
                          <span className="font-medium">Available Seats:</span>
                          <span className={`font-bold ${getSeatRemainingColor()}`}>
                            {Math.max(0, (companyInfo?.max_employee_capacity || 0) - (companyInfo?.employees_added || 0))} seats
                          </span>
                        </div>
                      </div>

                      {companyInfo?.seats_requested > companyInfo?.max_employee_capacity ? (
                        <div className="bg-yellow-100 dark:bg-yellow-950/40 text-yellow-800 dark:text-yellow-200 p-3 rounded-lg text-sm flex items-center justify-center font-medium border border-yellow-200">
                          <Clock className="w-4 h-4 mr-2 animate-pulse" />
                          Pending Approval ({companyInfo.seats_requested} Seats Requested)
                        </div>
                      ) : (
                        <Button className="w-full" onClick={openRequestSeatsModal}>
                          Request More Seats
                        </Button>
                      )}
                    </CardContent>
                  </Card>
                </div>
              </div>
            </TabsContent>
            
            <TabsContent value="queries">
               <Card>
                 <CardHeader>
                   <CardTitle>Company Queries</CardTitle>
                   <CardDescription>Submit queries to the Nerdshive administration team</CardDescription>
                 </CardHeader>
                 <CardContent>